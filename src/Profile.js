import React from 'react';
import "./Profile.css"
import Navbar from './Navbar';

function Profile( {send} ) {
    return (
        <div>
            <h1>Profile</h1>
        </div>
    );
}

export default Profile;
